# 发布前检查清单（红线标准）

> **适用范围**：所有代码修改、功能新增、样式调整
> **检查时机**：每次 `git commit` 之前
> **责任人**：开发工程师 + 代码审查

---

## 🔴 红线检查项（一票否决）

### 1. SEO 影响检查
- [ ] **百度抓取不受影响**：修改不涉及 `<title>`、`<meta description>`、`<h1>` 等SEO关键元素
- [ ] **URL结构不变**：不修改现有的URL路径或文件名
- [ ] **sitemap.xml 无需更新**：没有新增或删除页面
- [ ] **robots.txt 无需更新**：爬取规则未变化
- [ ] **结构化数据完整**：Schema.org 标记未被破坏

**检查方法**：
```bash
# 检查关键SEO元素是否被修改
git diff HEAD~1 -- '*.html' | grep -E '(title>|description|h1>|canonical)'
```

### 2. 主框架稳定性检查
- [ ] **不修改核心布局**：`<header>`、`<nav>`、`<main>`、`<footer>` 结构不变
- [ ] **不修改关键CSS类**：`.container`、`.row`、`.col-*` 等布局类未被删除或重命名
- [ ] **不修改全局JavaScript**：不修改核心事件监听、路由逻辑、数据请求
- [ ] **不引入新的全局变量**：避免变量污染
- [ ] **不修改现有API接口**：后端接口调用方式不变

**检查方法**：
```bash
# 检查是否修改了关键结构
git diff HEAD~1 -- '*.html' | grep -E '(id="header"|id="footer"|class="container")'
```

### 3. 功能完整性检查
- [ ] **导航栏功能正常**：所有链接可点击、下拉菜单正常工作
- [ ] **表单提交正常**：所有表单可以正常提交
- [ ] **响应式布局正常**：桌面端、平板、移动端显示正常
- [ ] **浏览器兼容正常**：Chrome、Safari、Firefox 无报错

---

## 🟡 重要检查项（必须确认）

### 4. 代码质量检查
- [ ] **无语法错误**：JavaScript 无语法错误、CSS 无语法错误
- [ ] **无控制台报错**：浏览器 Console 无红色错误信息
- [ ] **无死链接**：所有内部链接可访问
- [ ] **图片加载正常**：所有图片路径正确，无 404

**检查方法**：
```bash
# 本地启动服务器预览
python3 -m http.server 8080 &
# 手动访问 http://localhost:8080 检查
```

### 5. 性能影响检查
- [ ] **无大文件提交**：单个文件不超过 1MB
- [ ] **无冗余代码**：删除 console.log、注释掉的代码
- [ ] **CSS/JS 未重复加载**：检查 `<head>` 中是否重复引入

---

## 🟢 建议检查项（优化空间）

### 6. 代码规范检查
- [ ] **缩进一致**：使用空格或 Tab（推荐2空格）
- [ ] **命名规范**：CSS 类使用 kebab-case，JS 变量使用 camelCase
- [ ] **注释清晰**：复杂逻辑有注释说明

---

## 📋 发布前标准化流程

### Step 1: 代码自查（开发者）
```bash
# 1. 查看修改内容
git diff

# 2. 检查SEO关键元素
git diff -- '*.html' | grep -E '(title>|description|h1>)'

# 3. 本地预览测试
python3 -m http.server 8080
# 手动访问 http://localhost:8080 检查功能

# 4. 检查控制台报错
# 打开浏览器开发者工具，查看 Console 选项卡
```

### Step 2: 提交代码
```bash
# 1. 暂存修改
git add -A

# 2. 提交（附上详细的提交信息）
git commit -m "feat/fix: 简要描述

- 详细修改点1
- 详细修改点2
- SEO影响：无 / 有影响（说明）
- 主框架影响：无 / 有影响（说明）
"

# 3. 推送到远程
git push origin main
```

### Step 3: 发布后验证（必须）
```bash
# 1. 等待 GitHub Actions 部署完成（约2分钟）

# 2. 访问线上地址验证
# https://videotvai.com

# 3. 使用百度抓取诊断工具验证
# https://ziyuan.baidu.com/crawltools/index
```

---

## 🚨 应急预案

### 如果发现严重影响（立即回滚）
```bash
# 1. 回滚到上一个版本
git revert HEAD

# 2. 强制推送（谨慎使用）
git push origin main --force

# 3. 通知相关人员
```

### 联系方式
- 技术负责人：[待填写]
- 产品负责人：[待填写]

---

## 📝 检查记录表（每次发布填写）

| 检查项 | 检查结果 | 检查人 | 时间 |
|--------|----------|--------|------|
| SEO影响 | ✅ 通过 / ❌ 未通过 | 张三 | 2026-05-09 |
| 主框架稳定性 | ✅ 通过 / ❌ 未通过 | 张三 | 2026-05-09 |
| 功能完整性 | ✅ 通过 / ❌ 未通过 | 张三 | 2026-05-09 |
| 代码质量 | ✅ 通过 / ❌ 未通过 | 张三 | 2026-05-09 |

---

## 🔄 持续改进

- 每次发布后记录问题
- 每月回顾检查清单
- 根据新发现问题更新清单

---

**最后更新**：2026-05-09
**版本**：v1.0
